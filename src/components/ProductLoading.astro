---

---

<div class="w-80 rounded-2xl shadow-md shadow-gray-800/50 animate-pulse">
    <div class="w-full h-52 rounded-t-2xl bg-gray-800/50"></div>

    <div>
        <h3 class="mb-5 font-[Barlow] text-xl text-center text-white/50">Cargando...</h3>

        <ul class="px-3">
            {
                Array(4).fill(0).map(()=> (
                    <li class="my-3 w-full h-6 bg-gray-800/50 rounded-full"></li>
                ))
            }
        </ul>

        <div class="w-16 h-6 mx-auto bg-gray-800/50 rounded-full"></div>

        <div class="w-24 h-6 mt-5 mx-auto bg-gray-800/50 rounded-full"></div>

        <button class="mt-5 w-full py-3 text-white/50 bg-gray-800/50 rounded-2xl font-bold active:scale-95 transition-[scale]">Cargando...</button>
    </div>
</div>